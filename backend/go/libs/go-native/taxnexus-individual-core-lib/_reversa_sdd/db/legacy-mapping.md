# Legacy Mapping — módulo `db`

> Gerado pelo **Arqueólogo** (Reversa) em 2026-06-10

| Símbolo | Tipo | Arquivo:linha | Notas |
|---------|------|---------------|-------|
| `ConnectPostgres` | função | `db/postgres.go:8` | `ParseConfig` (l.10) → `NewWithConfig` (l.14) |

Pacote: `db`. Dependência: `github.com/jackc/pgx/v5/pgxpool` (l.5). Configuração via `DATABASE_URL`.
