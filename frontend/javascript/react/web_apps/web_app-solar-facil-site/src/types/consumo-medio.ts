// Tipos para Consumo Médio — ≡ App
export interface ConsumoMedioType {
  id: number;
  name: string;
  initialValue: number;
  finalValue: number;
  unit: string;
  description: string;
  status: string;
}
